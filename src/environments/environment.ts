export const environment = {
  production: false,
  // Since the whole repo is at C:\xampp\htdocs\elite2-academy, the API
  // (dojo-api/) is reachable under that same path. If you instead copy
  // just the dojo-api folder to htdocs\dojo-api, change this back to
  // 'http://localhost/dojo-api/api'.
  apiUrl: 'http://localhost/elite2-academy/dojo-api/api',
};
